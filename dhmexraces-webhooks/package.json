{
  "name": "dhmexraces-webhooks",
  "version": "1.0.0",
  "description": "Webhook handler for DHMEXRACES Shopify store - sends confirmation emails via Resend",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "google-auth-library": "^10.5.0",
    "google-spreadsheet": "^3.3.0",
    "resend": "^2.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
